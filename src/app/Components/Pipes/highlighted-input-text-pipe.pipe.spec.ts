import { HighlightedInputTextPipePipe } from './highlighted-input-text-pipe.pipe';

describe('HighlightedInputTextPipePipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightedInputTextPipePipe();
    expect(pipe).toBeTruthy();
  });
});
